package booleanOperator

fun main() {
    var YourAge = 30
    var myAge = 30

//    if((myAge !== YourAge) && (myAge !== YourAge)) {
//        println(true)
//    }

    println((myAge !== YourAge) && (myAge !== YourAge))

    println("===========================")

    if((myAge == YourAge) || (myAge == YourAge)) {
        println(true)
    } else {
        println(false)
    }
}